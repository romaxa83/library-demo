# Demo library

> Тестовый проект для изучения FastAPI

## Что реализовано в проекте (Features)

На проекте реализованно REST API, код протестирован, используются статические анализаторы и линтеры для форматирования кодовой базы
Роуты, запросы к бд и тесты написаны в асинхроном стиле.
### 🚀 Основной функционал
*   **Реализованна модуль пользователя**
    *   Регистрация и логин пользователя, получения авторизованного пользователя
    *   подтверждение почты, восстановления пароля (письма в режиме разработки приходят на `http://127.0.0.1:8025/`)
    *   Реализованы связи: user->role->permissions

*   **Реализованна ролевая система для пользователей(Role, Permission)**
    *   Созданы и описаны роуты для создания/редактирование/просмотр модели/получение списка ролей.
    *   Получение списка разрешений
    *   Реализованы связи: многие-ко-многим(role<->permission), через промежуточную таблицу.

*   **Реализованы модели (Book, Author)**
    *   Созданы и описаны роуты для создания/редактирование/просмотр модели/получение списка с пагинацией/удаление(soft, force)/восстановление.
    *   Фильтрация и сортировка списков.
    *   Валидация входных данных.
    *   Загрузка картинок
    *   Выгрузка данных (excel, csv)
    *   Реализованы *feature* тесты для роутов.
    *   Реализованы связи: один-ко-многим(book->author), многие-к-одному (author->books), полиморфная связь (book->media).

### 🚀 Дополнительный функционал
* подключены кастомные middleware для получении времени работы роута а также статистика запросов по роутам 
и их статусам (/info)

### 🧰 Используемые пакеты

*   **Для миграций базы данных:** **Alembic**.
*   **Описание моделей и валидации данных:** Pydantic.
*   **Запросы к бд:** SQLAlchemy.
*   **Логирование:** Loguru.
*   **Тесты:** Pytest.
*   **Статические анализаторы:** pylint (глубокий анализ кода), flake8(стиль кода и ошибки),
*    black(автоматическое форматирование), isort(сортировка импортов), mypy(проверка типов).

## 🛠️ Технологии

*   [FastAPI](https://fastapi.tiangolo.com/)
*   [Faststream](https://fastapi.tiangolo.com/)
*   [PostgreSQL](https://www.postgresql.org/)
*   [RabbitMQ](https://www.postgresql.org/)
*   [Docker](https://www.docker.com/)

## ⚙️ Разворачивание и запуск приложения

используется утилита make
```bash
# копируем файл (создаст файлы: .env, .env.testing, docker-compose.yaml)
# for linux
make cp_file
#for mac
make cp_file os=mac

# Разворачиваем проект
make init_app

# Подымаем проект
make up

# Останавливаем проект
make down

# Список все команд
make

# запуск проекта (нативно)
python3 -m src.main
```

#### 🔹 Очереди

Для очередей (фоновых задач) используется связка Faststream и брокер RabbitMQ. После запуска приложения админка
rabbit находится `http://127.0.0.1:15672/`
```bash
# запуск очередей
make queue_start
# or
python -m faststream run src.faststream.app:app

# запуск документации (доступна по - http://127.0.0.1:8081/)
make queue_docs
# or 
python -m faststream docs serve src.faststream.app:app --port 8081
```

### Авторизация и аутентификация
##### Авторизация
Авторизация реализована через access_token и refresh_token (генерируются с помощью JWT на основе приватного 
и публичного ключа).Токены выдаются при логине и в дальнейшем передаются (для закрытых роутах) в загаловках:
`Authorization: Bearer access_token`. Токен не хранятся в бд а генерируются на лету.
Время жизни токена, как и другие настройки задаются в  `.env`, если используются дефолтный значения 
то указывать данные переменные окружения **не нужно**
```.env
# default = ./jwt-private.pem
AUTH_JWT_PUBLIC_KEY_PATH=
# default = ./jwt-public.pem
AUTH_JWT_PRIVATE_KEY_PATH=
# default = RS256
AUTH_JWT_ALGORITHM=
# minutes (default = 10min)
AUTH_JWT_ACCESS_TOKEN_EXPIRED=
# minutes (default = 7days)
AUTH_JWT_REFRESH_TOKEN_EXPIRED=
```
Также реализован метод для обновления токенов `/auth/refresh-tokens`

##### Аутентификация (RBAC)

Реализована ролевая система основана на ролях(`role`) и разрешения (`permission`). Пользователю присваивается
роль, а роли присваиваются разрешения. При доступе к ресурсу проверяется наличие нужно разрешения у роли авторизованого
пользователя. В системе есть две дефолтные роли: `user`и `superadmin`.

**Superadmin** создается в момент инициализации проекта или через команду `python -m cli.main seed superadmin`. 
<u>В системе может быть только один пользователь с ролью Superadmin</u>. Данная роль не имеет разрешений но ей доступны
все действия <u>без ограничений</u>. Пользователем с данной ролью нельзя манипулировать (удалять, редактировать, ...)
он также не попадает в список всех пользователей. При создании данного пользователя он получает дефолтные креды:
- admin@gmail.com
- password12

Но их можно изменить, указав перед созданием суперадмина в переменных окружения нужные креды (подробнее в настройках приложения)

**User** присваивается всем пользователям после регистрации, при создании можно выбирать роль (кроме роли `superadmin`).

Роли можно создавать свои, добавлять к ним разрешения и присваивать пользователям. Разрешения создавать нельзя, они
прописываются в ручную по мере появления новых акшенов. Разрешения прописываются в `src/rbac/permissions.py` в классе
`Permissions` а также добавляются в `get_permissions_for_seed()` для загрузки в бд, загружаются в бд командой:
```bash
python -m cli.main seed perms
```

Данная команда загружает не только разрешения но и дефолтные роли (если их еще нет), и связь между дефолтными ролями и 
разрешениями (которые прописываются в `get_permissions_for_roles()`). Можно добавлять свои дефолтные роли прописывать им
разрешения или убирать разрешения, и загружать через команду выше

Доступ пользователей только к собственным ресурсам (в разработке) 

### Безопасность
- CORS

На проекте подключен мидлевар для cors, настройки для него можно прописать в переменных окружения (подробнее в настройках приложения)
проверить наличие заголовков можно командой (используется утилита `httpie`)
```bash
http OPTIONS http://localhost:8000/auth/login 'Access-Control-Request-Method:GET' 'Origin: http://localhost:8000'
```